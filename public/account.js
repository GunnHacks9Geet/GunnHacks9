window.onload = () => {
    //TODO (high priority) load a list of the current assignment types into a paragraph
    //the p tag has an id of 'currentAssignments'

    //TODO (high priority) add functionality to the button so it 1) REDIRECTS to the other page
    // 2) the data entered into the form (i.e. assignment type and how long it takes on average)
    // needs to be put into localStorage and LOADED into the ADD-TASK.HTML FILE under one of the dropdown menus

    document.getElementById("addAssignment").addEventListener("click", () => {

    })
}